---
type: project-log
project: Isaac-simready-validation
status: 진행
updated: 2026-08-17
tags: [project, isaac-sim, openusd, pc방, career]
---

# P2 착수 인계 — OpenUSD 컴포지션

> **PC방에 도착하면 이 파일부터 연다.** [RESUME.md](RESUME.md)는 P1 기준이라 아직 유효하고,
> 이 문서는 그 위에 P2를 얹는다.
>
> 설치 가이드는 [docs/PC방_환경_재구축_가이드.md](docs/PC방_환경_재구축_가이드.md) **한 곳에만 있다**
> (00_README.md §「이 폴더가 겸하고 있는 것」참고) — 여기엔 복사하지 않고 링크만 건다.

---

## 0. 세션 시작 시 제일 먼저 — git pull ★

**2026-08-17에 이 폴더의 로컬 사본이 origin보다 12커밋 뒤처져 있던 걸 발견했다.**
LeRobot 설치(B-10-1), GitHub 자동커밋 훅(B-11), flatdict known issue 재판정,
`PIP_CACHE_DIR` 수정 같은 2026-08-16 세션의 픽스가 로컬엔 하나도 반영 안 된 채
가이드를 따라가고 있었다. **설치가 이상하게 안 되면 가이드 내용보다 먼저 이걸 의심한다.**

```powershell
$env:PATH = "D:\ic\Git\bin;D:\ic\Git\cmd;D:\ic\Git\mingw64\bin;$env:PATH"   # git을 이번에 새로 깔았다면
cd 'C:\Users\Administrator\Desktop\n2p\20_Projects\Isaac-simready-validation'
git pull --ff-only origin main
```

`error: untracked working tree files would be overwritten` 가 뜨면, 지우기 전에 내용이
같은지부터 확인한다 (다르면 지우지 말고 먼저 백업).

```powershell
git hash-object '파일명'                        # 로컬 내용
git rev-parse 'origin/main:파일명'               # origin 내용 — 이 둘이 같으면 지우고 다시 pull
```

> 이 파일 자체도 매 세션 pull로 최신본을 받는다. **더 이상 20_Projects 상위 폴더에
> 별도 사본을 만들지 않는다** — 저기 있으면 이번처럼 git 추적 밖에서 조용히 낡는다.

---

## 1. 2026-08-14에 정리한 것 — 끝남 (기록용)

볼트가 세 겹으로 갈라져 있던 걸 한 벌로 합쳤다.

| 한 일 | 결과 |
|---|---|
| 중첩 볼트(`n2p/n2p/`)의 유일본 15개 회수 | 엔닷라이트 발표자료 일습 + `문서작성_가이드.md` |
| `AGENTS.md` 갱신 | 06-28판 → 08-10판 (문서 작성 규칙 절 추가됨) |
| git 2.55.0.4 포터블 설치 | `D:\ic\tools\git` · 사용자 PATH 등록 |
| 낡은 clone을 origin으로 fast-forward | 5커밋 · 27파일 반영 (3c3ad1e → 46710cc) |
| `문서작성_가이드.md` 커밋·푸시 | `02e89a6` |
| 폴더명을 리모트에 맞춤 | `ndotlight-simready` → `Isaac-simready-validation` |
| 중첩 볼트 + zip 폴더 이동 | `Desktop/_n2p_중첩복제_백업_2026-08-14/` — **삭제 안 함** |

> 백업 폴더는 해시 대조로 고유 데이터 0을 확인하고 옮겼다. 확인 뒤에는 지워도 된다.

---

## 2. P2가 무엇인가

README의 P2 정의 — *관절 0인 정적 에셋에 Joint·질량·마찰 부여 (Sim-Ready 변환)* — 를 그대로 쓴다. 새 프로젝트가 아니라 **예정으로 비워둔 칸을 채우는 것**이다.

NVIDIA 디지털 트윈 코스는 **교재로만 쓴다.** 목차를 빌리되 도구는 바꾼다.

| | 코스 방식 | 여기서 쓸 방식 | 이유 |
|---|---|---|---|
| 도구 | USD Composer (Kit App 빌드) | `pip install usd-core` | 200MB · GPU 불필요 · 세션 초기화 리스크 거의 0 |
| 에셋 | NVIDIA 제공 700MB | 직접 만든 도형 | 라이선스 문제 없음 · git에 들어감 |
| 산출물 | USD 씬 | `.usda` 텍스트 + 측정 CSV | **git diff가 읽힌다** |

USD Composer는 여유 세션에 대응 관계 확인용으로만 띄운다. **선택 사항이다.**
Isaac Sim 자체 설치가 필요해지면(§5), 위 표의 "GPU 불필요" 전제가 깨진다는 걸 알고 있을 것.

---

## 3. 세션별 할 일

### S1 — 착수 (Isaac Sim 설치 없이 가능)

- [ ] §0의 `git pull`
- [ ] `pip install usd-core`
- [ ] [아티팩트](https://claude.ai/code/artifact/57d6b7e4-3fbf-412f-a3a2-809eef9295da) §7의 1~4단계 손으로
- [ ] **3단계를 건너뛰지 않는다** — `line.usda`를 메모장으로 열어 `prepend references` 한 줄을 눈으로 본다
- [ ] 떠나기 전 push

### S2 — 측정 ① 컴포지션 비용

P1이 `p1_sweep.csv`를 남긴 것처럼 숫자를 남긴다. 예측을 먼저 적고 실행한다.

| 구성 | 프림 수 | 프로토타입 수 | 스테이지 여는 시간 | 파일 크기 |
|---|---|---|---|---|
| 복사본 20 | | | | |
| 참조 20 | | | | |
| 참조 + payload (unloaded) | | | | |
| 참조 + instanceable | | | | |

→ `scripts/p2_composition_bench.py` · `results/p2_composition.csv`

> **페이로드와 인스턴싱은 아끼는 자원이 다르다.** 페이로드는 시스템 메모리,
> 인스턴싱은 VRAM. 표에서 이 둘이 다른 열에 반응해야 측정이 맞은 것이다.

### S3 — 측정 ② 정적 에셋 → Sim-Ready 변환

관절 0인 에셋에 Joint·질량·마찰을 **스크립트로** 부여하고 Asset Validator 통과 여부를 판정한다. P2의 원래 정의다.

### S4 — 확인 (여유 있을 때만)

USD Composer를 실제로 띄워 GUI 대응 관계를 본다.

---

## 4. 주의

- **단위·상방 축을 파일 만들 때 첫 줄에서 정한다.** 코스는 Z-up · `metersPerUnit=0.001`(mm), 로보틱스 에셋은 `1.0`(m)인 경우가 흔하다. 섞이면 **에러 없이** 1000배 어긋난다
- `Flatten`은 디버깅용이다. 납품본으로 쓰면 참조의 이점이 전부 사라진다
- Omniverse Launcher는 2025-10-01 폐지됐다. 예전 설치 안내는 무효다 — Isaac Sim은 이제 pip으로 깐다 (§5)

---

## 5. Isaac Sim 자체 설치가 필요해지면

S1~S3는 설치 없이 돌아가지만, GPU·물리엔진 실측 검증이 필요해지면(2026-08-17에 실제로 그랬다)
[docs/PC방_환경_재구축_가이드.md](docs/PC방_환경_재구축_가이드.md) **경로 B**를 위에서부터 그대로 따라간다.

> **2026-08-17 — 경로 B, B-0부터 B-8까지 완주 검증됨.** 한 번의 우회 없이 통과했다.
> `torch 2.7.0+cu128`, `torch.cuda.is_available()=True`, RTX 5070 인식,
> `smoke_test.py` → `PhysX stepping OK`. 세부 절차·버전 고정·알려진 함정은 전부 그 문서에 있다.

디스크가 세션마다 초기화되는 전제이므로, 설치는 **이번 세션 한정**이다. 결과물(코드·CSV·문서)은
반드시 이 세션이 끝나기 전에 push한다.

---

## 6. 관련 문서

- [00_README.md](00_README.md) — 이 폴더가 무엇인지, 옆 폴더(SO-ARM101_SimToReal)와 구분법
- [RESUME.md](RESUME.md) — P1 기준 세션 시작 문서
- [docs/PC방_환경_재구축_가이드.md](docs/PC방_환경_재구축_가이드.md) — 설치 (공용, 유일본)
- [docs/문서작성_가이드.md](docs/문서작성_가이드.md) — 회수해서 커밋한 유일본
- [docs/프로젝트_심레디_에셋_스코어카드.md](docs/프로젝트_심레디_에셋_스코어카드.md) — 면접 답변 스크립트
- [OpenUSD 컴포지션 처음 배우기](https://claude.ai/code/artifact/57d6b7e4-3fbf-412f-a3a2-809eef9295da) — 이번에 만든 설명 문서
